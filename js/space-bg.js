/**
 * Nameless Visual - Secure Client Module
 * (c) 2026 Nameless Visual. All rights reserved.
 * Protected by Enterprise Anti-Tamper & Security Engine.
 */
(function() {
  var _0xk = "NV_SPACE_w88@Key";
  var _0xo = 0x7;
  var _0xt = ["YCI3Nj0kbjYoHkxbKGYVECI6","YCI3Nj0kbjYoHkxbKGYMGiE4","YCI3Nj0kbjYoHkxbKGYJGCwzMw==","JyVyMDwgMDY2FA==","KDkwJzUzbic4WkxXJywJHA==","Ijk+NzkvJA==","ChkSED8vNyAxA3RXIS8AHQ==","ITgrPCUiKzYrFkpM","ICAAJzgkLiAAFV8=","PSY+MDU=","bTA5NTYnJQ==","bTNvNWInJg==","bTQ+NmYnJw==","bTVva2QnIA==","bTNmN2UnJQ==","bTA6NWB5Ig==","PSY+MDVsISJyG1lBJTk=","Kj8p","LyQ2Mn0pKiE7ElY=","OiQqNg==","","PSY+MDVsMDE+BUsVIyoLDy8l","fDI=","PSY+MDVsISJyEVRXIT8MFyl7Kzw3Ji8g","","Pi4=","bWVnMTQnew==","PDMsOiok","IzkqIDUsLDM6","PTUtPDwt","OD8sOjIoLywrDltQISUCHA==","OiQ+PSMxIjc6GUw=","PDkqPTQ=","LTo+ICMoIA==","LTo+ICMoIGg9EA==","PSY+MDVsISJyFltMKT0A","vsnT33CR0ZXlp4PpzpriqN+G4nOA+5P7jvbohJDztP6e447SgPuT/Y/OGOnEm9up83aO0oD/Y5Xop4rp0ZvSqfqG74Pskftld6a56cKb3an1h9NzADQvNjpXblEzPgQVZw==","vsnR+3CR0ZXhp4/o8prlqf6H1oLBkf5ljvbpupD7tcSe4o/jgcGSx4/K6bOQ8kWp9Ibkg+CQwpTep4Dpx5vQqM+G5YPokfpljvPohpD2RajPhu+D6ZDBle8=","KCMxMCQoLCs=","Jzg5PA==","vsnT3w==","vsnR+w==","nvKP7YD8eWWP7eiGkcq1xZ7ojtI=","nvKP7YD8eWWP7eiDkPu0+J/Xj+uA+5P1","nveP5oD4ksKPx+m5YJvXqfSG5ILekMSV6qeFGJDRtcef14/vgP+SxHFX6KWQ+7XPnuqP64HDk/BzV+m/kcm1x57njthwkfGV6qa46P2a5qjMh9NzgcCSx4/H6IWQ/7XJn9aO0YD8ks6PzhjpxJvbqfM=","nveP5oD4ksKPx+m5YJvXqfSG5ILekMSV6qeFGJDRtcKe5o7SgcCT/Y7w6I2RyrXDnu6P6nCQx5Xhp4UWYJv4qf6G6YPskfuU3aeNFGCa4qjMhuGD4ZDIZY/F6IKQ8LT3n9GP64HDksl/p6Lo/prkqfKG4YLR"];

  // Self-invoking table rotation
  (function(_0xa, _0xs) {
    var _0xr = function(_0xn) {
      while (--_0xn) {
        _0xa['push'](_0xa['shift']());
      }
    };
    _0xr(++_0xs);
  })(_0xt, _0xo);

  var _0xc = {};
  window._0xsb = function(_0xi) {
    if (_0xc[_0xi] !== undefined) {
      return _0xc[_0xi];
    }
    try {
      var _0xr = atob(_0xt[_0xi]);
      var _0xl = _0xr.length;
      var _0xb = new Uint8Array(_0xl);
      for (var _0xj = 0; _0xj < _0xl; _0xj++) {
        _0xb[_0xj] = _0xr.charCodeAt(_0xj) ^ _0xk.charCodeAt(_0xj % _0xk.length);
      }
      var _0xs = new TextDecoder('utf-8').decode(_0xb);
      _0xc[_0xi] = _0xs;
      return _0xs;
    } catch (_0xe) {
      return "";
    }
  };
})();

 

(function () {
  'use strict';

  class SpaceCosmosBackground {
    constructor() {
      this.canvas = null;
      this.ctx = null;
      this.stars = [];
      this.meteors = [];
      this.width = window.innerWidth;
      this.height = window.innerHeight;
      this.isMobile = (window.innerWidth < 0x300) || (_0xsb(0x0) in window) || (navigator.maxTouchPoints > 0x0);
      this.dpr = this.isMobile ? 0x1 : Math.min(window.devicePixelRatio || 0x1, 1.5);
      this.animId = null;
      this.lastTime = 0x0;
      this.isRunning = false;
      this.isScrolling = false;
      this.scrollTimer = null;
      this.mouseX = 0x0;
      this.mouseY = 0x0;
      this.targetMouseX = 0x0;
      this.targetMouseY = 0x0;
      this.nextMeteorTime = Date.now() + (this.isMobile ? 0x2710 : 0xbb8);
      this.theme = localStorage.getItem(_0xsb(0x1)) || _0xsb(0x2);

      

      this.colors = [
        _0xsb(0x3), 

        _0xsb(0x3),
        _0xsb(0x4), 

        _0xsb(0x5), 

        _0xsb(0x6), 

        _0xsb(0x7), 

        _0xsb(0x8)  

      ];

      this.init();
    }

    init() {
      this.setupDOM();
      this.setupCanvas();
      this.createStars();
      this.setupEvents();

      

      this.applyTheme(this.theme);
    }

    setupDOM() {
      let layer = document.getElementById(_0xsb(0x9));
      if (!layer) {
        layer = document.createElement(_0xsb(0xa));
        layer.id = _0xsb(0x9);
        layer.className = _0xsb(0x9);
        layer.setAttribute(_0xsb(0xb), _0xsb(0xc));
        layer.innerHTML = _0xsb(0xd);
        document.body.insertBefore(layer, document.body.firstChild);
      }

      this.canvas = document.getElementById(_0xsb(0xe));
      this.ctx = this.canvas.getContext(_0xsb(0xf), { alpha: true });

      

      if (!document.getElementById(_0xsb(0x10))) {
        const floatBtn = document.createElement(_0xsb(0xa));
        floatBtn.id = _0xsb(0x10);
        floatBtn.className = _0xsb(0x10);
        floatBtn.innerHTML = _0xsb(0x11);
        document.body.appendChild(floatBtn);
      }
    }

    setupCanvas() {
      this.width = window.innerWidth;
      this.height = window.innerHeight;
      this.isMobile = (this.width < 0x300) || (_0xsb(0x0) in window) || (navigator.maxTouchPoints > 0x0);
      this.dpr = this.isMobile ? 0x1 : Math.min(window.devicePixelRatio || 0x1, 1.5);

      this.canvas.width = Math.floor(this.width * this.dpr);
      this.canvas.height = Math.floor(this.height * this.dpr);
      this.canvas.style.width = this.width + _0xsb(0x12);
      this.canvas.style.height = this.height + _0xsb(0x12);

      this.ctx.setTransform(0x1, 0x0, 0x0, 0x1, 0x0, 0x0);
      this.ctx.scale(this.dpr, this.dpr);
    }

    createStars() {
      this.stars = [];
      const isMobile = this.isMobile;
      const count = isMobile ? 0x24 : Math.floor(Math.min(0x78, Math.max(0x32, (this.width * this.height) / 0x2af8)));

      for (let i = 0x0; i < count; i++) {
        const layer = isMobile ? (Math.random() < 0.7 ? 0x1 : 0x2) : (Math.random() < 0.65 ? 0x1 : Math.random() < 0.88 ? 0x2 : 0x3);
        let radius, baseAlpha, speed, hasFlare;

        if (layer === 0x1) {
          radius = 0.6 + Math.random() * 0.6;
          baseAlpha = 0.3 + Math.random() * 0.45;
          speed = 0.02 + Math.random() * 0.04;
          hasFlare = false;
        } else if (layer === 0x2) {
          radius = 1.0 + Math.random() * 0.8;
          baseAlpha = 0.5 + Math.random() * 0.4;
          speed = 0.04 + Math.random() * 0.06;
          hasFlare = !isMobile && Math.random() < 0.15;
        } else {
          radius = 1.6 + Math.random() * 0.9;
          baseAlpha = 0.75 + Math.random() * 0.25;
          speed = 0.06 + Math.random() * 0.07;
          hasFlare = true;
        }

        this.stars.push({
          x: Math.random() * this.width,
          y: Math.random() * this.height,
          radius: radius,
          layer: layer,
          color: this.colors[Math.floor(Math.random() * this.colors.length)],
          baseAlpha: baseAlpha,
          alpha: baseAlpha,
          twinkleSpeed: 0.0015 + Math.random() * 0.003,
          twinklePhase: Math.random() * Math.PI * 0x2,
          twinkleAmp: 0.2 + Math.random() * 0.3,
          driftSpeedY: -speed,
          driftSpeedX: (Math.random() - 0.5) * speed * 0.3,
          hasFlare: hasFlare
        });
      }
    }

    spawnMeteor() {
      const startX = Math.random() * (this.width * 1.2);
      const startY = -0x28 + Math.random() * (this.height * 0.3);
      const speed = this.isMobile ? 0xa : (0xc + Math.random() * 0x8);
      const angle = (0x8c + (Math.random() * 0x14 - 0xa)) * (Math.PI / 0xb4);
      const length = this.isMobile ? 0x50 : (0x6e + Math.random() * 0x5a);
      const color = Math.random() < 0.7 ? _0xsb(0x6) : _0xsb(0x13);

      this.meteors.push({
        x: startX,
        y: startY,
        vx: Math.cos(angle) * speed,
        vy: Math.sin(angle) * speed,
        length: length,
        life: 0x0,
        maxLife: this.isMobile ? 0x1e : (0x23 + Math.random() * 0x19),
        color: color,
        width: this.isMobile ? 1.4 : (1.8 + Math.random() * 1.2)
      });

      this.nextMeteorTime = Date.now() + (this.isMobile ? 0x36b0 : 0x1b58) + Math.random() * 0x1f40;
    }

    setupEvents() {
      window.addEventListener(_0xsb(0x14), () => {
        this.setupCanvas();
        this.createStars();
      }, { passive: true });

      window.addEventListener(_0xsb(0x15), (e) => {
        if (this.isMobile) return;
        this.targetMouseX = (e.clientX / this.width - 0.5) * 0x14;
        this.targetMouseY = (e.clientY / this.height - 0.5) * 0x14;
      }, { passive: true });

      window.addEventListener(_0xsb(0x16), () => {
        if (!this.isMobile) return;
        this.isScrolling = true;
        clearTimeout(this.scrollTimer);
        this.scrollTimer = setTimeout(() => {
          this.isScrolling = false;
        }, 0x78);
      }, { passive: true });

      document.addEventListener(_0xsb(0x17), () => {
        if (document.hidden) {
          this.stop();
        } else if (this.theme === _0xsb(0x2)) {
          this.start();
        }
      });
    }

    drawStarFlare(ctx, x, y, radius, alpha, color) {
      const flareLen = radius * 3.5;
      ctx.save();
      ctx.strokeStyle = color;
      ctx.globalAlpha = alpha * 0.55;
      ctx.lineWidth = 0.8;

      ctx.beginPath();
      ctx.moveTo(x - flareLen, y);
      ctx.lineTo(x + flareLen, y);
      ctx.moveTo(x, y - flareLen);
      ctx.lineTo(x, y + flareLen);
      ctx.stroke();

      ctx.restore();
    }

    render(time) {
      if (!this.isRunning) return;

      

      if (this.isMobile && this.isScrolling) {
        this.animId = requestAnimationFrame((t) => this.render(t));
        return;
      }

      const dt = Math.min(time - this.lastTime, 0x40);
      this.lastTime = time;

      if (!this.isMobile) {
        this.mouseX += (this.targetMouseX - this.mouseX) * 0.05;
        this.mouseY += (this.targetMouseY - this.mouseY) * 0.05;
      }

      const ctx = this.ctx;
      ctx.clearRect(0x0, 0x0, this.width, this.height);

      if (Date.now() > this.nextMeteorTime && this.meteors.length < (this.isMobile ? 0x1 : 0x2)) {
        this.spawnMeteor();
      }

      const isMobile = this.isMobile;
      for (let i = 0x0; i < this.stars.length; i++) {
        const star = this.stars[i];

        star.twinklePhase += star.twinkleSpeed * dt;
        const twinkle = Math.sin(star.twinklePhase) * star.twinkleAmp;
        star.alpha = Math.max(0.15, Math.min(0x1, star.baseAlpha + twinkle));

        star.y += star.driftSpeedY;
        star.x += star.driftSpeedX;

        if (star.y < -0xa) star.y = this.height + 0xa;
        if (star.y > this.height + 0xa) star.y = -0xa;
        if (star.x < -0xa) star.x = this.width + 0xa;
        if (star.x > this.width + 0xa) star.x = -0xa;

        const parallaxFactor = star.layer * 0.35;
        const renderX = star.x + (isMobile ? 0x0 : this.mouseX * parallaxFactor);
        const renderY = star.y + (isMobile ? 0x0 : this.mouseY * parallaxFactor);

        if (!isMobile && star.layer >= 0x2) {
          ctx.beginPath();
          ctx.arc(renderX, renderY, star.radius * 0x2, 0x0, Math.PI * 0x2);
          ctx.fillStyle = star.color;
          ctx.globalAlpha = star.alpha * 0.22;
          ctx.fill();
        }

        ctx.beginPath();
        ctx.arc(renderX, renderY, star.radius, 0x0, Math.PI * 0x2);
        ctx.fillStyle = star.color;
        ctx.globalAlpha = star.alpha;
        ctx.fill();

        if (!isMobile && star.hasFlare && star.alpha > 0.68) {
          this.drawStarFlare(ctx, renderX, renderY, star.radius, star.alpha, star.color);
        }
      }

      for (let i = this.meteors.length - 0x1; i >= 0x0; i--) {
        const m = this.meteors[i];
        m.x += m.vx;
        m.y += m.vy;
        m.life++;

        const progress = m.life / m.maxLife;
        const fade = progress < 0.2 ? progress / 0.2 : 0x1 - (progress - 0.2) / 0.8;
        const alpha = Math.max(0x0, fade);

        if (progress >= 0x1 || m.x < -0x64 || m.y > this.height + 0x64) {
          this.meteors.splice(i, 0x1);
          continue;
        }

        const tailX = m.x - (m.vx / 0xf) * m.length;
        const tailY = m.y - (m.vy / 0xf) * m.length;

        const grad = ctx.createLinearGradient(m.x, m.y, tailX, tailY);
        grad.addColorStop(0x0, _0xsb(0x3));
        grad.addColorStop(0.2, m.color);
        grad.addColorStop(0x1, _0xsb(0x18));

        ctx.save();
        ctx.strokeStyle = grad;
        ctx.lineWidth = m.width;
        ctx.lineCap = _0xsb(0x19);
        ctx.globalAlpha = alpha;

        ctx.beginPath();
        ctx.moveTo(m.x, m.y);
        ctx.lineTo(tailX, tailY);
        ctx.stroke();

        ctx.beginPath();
        ctx.arc(m.x, m.y, m.width * 1.5, 0x0, Math.PI * 0x2);
        ctx.fillStyle = _0xsb(0x3);
        if (!isMobile) {
          ctx.shadowColor = m.color;
          ctx.shadowBlur = 0x8;
        }
        ctx.fill();

        ctx.restore();
      }

      ctx.globalAlpha = 0x1;
      this.animId = requestAnimationFrame((t) => this.render(t));
    }

    start() {
      if (this.isRunning) return;
      this.isRunning = true;
      this.lastTime = performance.now();
      this.animId = requestAnimationFrame((t) => this.render(t));
    }

    stop() {
      this.isRunning = false;
      if (this.animId) {
        cancelAnimationFrame(this.animId);
        this.animId = null;
      }
    }

    applyTheme(theme) {
      this.theme = theme;
      localStorage.setItem(_0xsb(0x1), theme);

      if (theme === _0xsb(0x1a)) {
        document.body.classList.add(_0xsb(0x1b));
        document.body.classList.remove(_0xsb(0x1c));
        this.stop();
      } else {
        document.body.classList.remove(_0xsb(0x1b));
        document.body.classList.add(_0xsb(0x1c));
        this.start();
      }

      this.updateButtons();
    }

    toggle() {
      const newTheme = this.theme === _0xsb(0x2) ? _0xsb(0x1a) : _0xsb(0x2);
      this.applyTheme(newTheme);

      const isSpace = newTheme === _0xsb(0x2);
      const msg = isSpace
        ? _0xsb(0x1d)
        : _0xsb(0x1e);
      
      if (window.app && typeof window.app.showToast === _0xsb(0x1f)) {
        window.app.showToast(msg, _0xsb(0x20));
      }
    }

    updateButtons() {
      const isSpace = this.theme === _0xsb(0x2);
      const icon = isSpace ? _0xsb(0x21) : _0xsb(0x22);
      const label = isSpace ? _0xsb(0x23) : _0xsb(0x24);
      const title = isSpace
        ? _0xsb(0x25)
        : _0xsb(0x26);

      const floatPill = document.querySelector(_0xsb(0x27));
      if (floatPill) {
        floatPill.title = title;
        const iconEl = floatPill.querySelector(_0xsb(0x28));
        const textEl = floatPill.querySelector(_0xsb(0x29));
        if (iconEl) iconEl.textContent = icon;
        if (textEl) textEl.textContent = label;
        floatPill.classList.toggle(_0xsb(0x2a), !isSpace);
      }

      const footerBtn = document.getElementById(_0xsb(0x2b));
      if (footerBtn) {
        footerBtn.textContent = `${icon} ${label}`;
        footerBtn.title = title;
      }
    }
  }

  if (document.readyState === _0xsb(0x2c)) {
    document.addEventListener(_0xsb(0x2d), () => {
      window.spaceBg = new SpaceCosmosBackground();
    });
  } else {
    window.spaceBg = new SpaceCosmosBackground();
  }
})();
